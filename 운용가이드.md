# AI 영수증 지출 관리 시스템 — 운용 가이드

---

## 1. 접속 URL

| 환경 | URL |
|------|-----|
| **Vercel 배포 (프론트엔드)** | https://yunpapa-g6jdc8e2l-jhh0515-7562s-projects.vercel.app |
| 로컬 프론트엔드 | http://localhost:5173 |
| 로컬 백엔드 API | http://localhost:8000 |
| 로컬 Swagger UI | http://localhost:8000/docs |
| GitHub 저장소 | https://github.com/yunpapa/yunpapa |
| Vercel 대시보드 | https://vercel.com/dashboard |

---

## 2. 로컬 개발 환경 실행

### 백엔드

```bash
cd backend

# 최초 1회: 패키지 설치 (반드시 Anaconda Python 사용)
C:/Users/Jhh0515/Anaconda3/Scripts/pip.exe install -r requirements.txt

# 서버 실행
C:/Users/Jhh0515/Anaconda3/python.exe -m uvicorn app.main:app --reload --port 8000
```

> **주의**: 시스템 기본 Python(3.14)은 일부 패키지 빌드 오류가 있어 **Anaconda Python(3.13)** 을 사용해야 합니다.

### 프론트엔드

```bash
cd frontend

# 최초 1회: 패키지 설치
npm install

# 개발 서버 실행
npm run dev
```

### 환경 변수 파일 위치

| 파일 | 필수 변수 |
|------|-----------|
| `backend/.env` | `UPSTAGE_API_KEY`, `DATABASE_URL`, `UPLOAD_DIR`, `ALLOWED_ORIGINS` |
| `frontend/.env` | `VITE_API_BASE_URL` (없으면 http://localhost:8000 기본값 사용) |

---

## 3. Git 커밋 & 푸시 방법

### 일반적인 작업 흐름

```bash
# 1. 변경 사항 확인
git status

# 2. 파일 스테이징
git add <파일명>         # 특정 파일
# 또는
git add backend/ frontend/   # 디렉토리 단위

# 3. 커밋
git commit -m "feat: 기능 설명"

# 4. GitHub에 푸시 → Vercel 자동 배포 트리거
git push origin main
```

### 커밋 메시지 규칙 (권장)

| 접두사 | 사용 상황 |
|--------|-----------|
| `feat:` | 새 기능 추가 |
| `fix:` | 버그 수정 |
| `docs:` | 문서 변경 |
| `refactor:` | 리팩토링 |
| `chore:` | 빌드/설정 변경 |

### 절대 커밋하면 안 되는 파일

```
backend/.env        ← UPSTAGE_API_KEY 포함
.env                ← GITHUB_API_KEY 포함
backend/receipt.db  ← 데이터베이스
backend/uploads/    ← 업로드된 이미지
```

> `.gitignore`에 이미 등록되어 있어 `git add .` 해도 자동으로 제외됩니다.

---

## 4. Vercel 자동 배포 구조

```
로컬에서 git push origin main
        │
        ▼
GitHub (yunpapa/yunpapa) — main 브랜치 push 감지
        │
        ▼  Webhook 트리거
Vercel CI/CD
        │  npm run build (Vite)
        ▼
Vercel CDN 배포 완료 (약 1~2분 소요)
        │
        ▼
https://yunpapa-g6jdc8e2l-jhh0515-7562s-projects.vercel.app
```

### 배포 상태 확인

- **Vercel 대시보드**: https://vercel.com/dashboard → 프로젝트 선택 → Deployments 탭
- **GitHub**: 저장소 → Actions 또는 커밋 옆 ✅/❌ 아이콘으로 확인

---

## 5. Vercel 환경 변수 관리

Vercel에 배포된 앱의 환경 변수는 코드가 아닌 **Vercel 대시보드**에서 관리합니다.

```
Vercel 대시보드 → 프로젝트 선택 → Settings → Environment Variables
```

### 프론트엔드 프로젝트에 필요한 환경 변수

| 변수명 | 예시 값 |
|--------|---------|
| `VITE_API_BASE_URL` | `https://your-backend.vercel.app` |

### 백엔드 프로젝트에 필요한 환경 변수

| 변수명 | 예시 값 |
|--------|---------|
| `UPSTAGE_API_KEY` | `up_AGIwlKOSMoRp2X...` |
| `DATABASE_URL` | `sqlite:////tmp/receipt.db` |
| `UPLOAD_DIR` | `/tmp/uploads` |
| `ALLOWED_ORIGINS` | `https://yunpapa-xxx.vercel.app` |

> 환경 변수 변경 후에는 **Redeploy** 버튼을 눌러야 적용됩니다.

---

## 6. Vercel 서버리스 환경 주의사항

현재 백엔드를 Vercel에 배포할 경우 다음 제약이 있습니다.

| 항목 | 내용 |
|------|------|
| **데이터베이스** | `/tmp/receipt.db` 사용 — 서버 재시작 시 데이터 초기화 |
| **업로드 파일** | `/tmp/uploads/` 사용 — 서버 재시작 시 이미지 삭제 |
| **함수 실행 시간** | 최대 10초 (OCR 처리 시 초과 가능) |

### 영구 저장이 필요한 경우 권장 대안

- **백엔드 서버**: [Railway](https://railway.app) 또는 [Render](https://render.com) 로 이전 (파일시스템 유지)
- **데이터베이스**: [Supabase](https://supabase.com) (PostgreSQL 무료 플랜)
- **파일 저장소**: [Cloudflare R2](https://cloudflare.com/r2) 또는 AWS S3

---

## 7. 자주 쓰는 명령어 모음

```bash
# ── 로컬 서버 ─────────────────────────────────────────
# 백엔드 시작
cd backend && C:/Users/Jhh0515/Anaconda3/python.exe -m uvicorn app.main:app --reload --port 8000

# 프론트엔드 시작
cd frontend && npm run dev

# 백엔드 테스트 실행
cd backend && C:/Users/Jhh0515/Anaconda3/python.exe -m pytest tests/ -v

# ── Git ───────────────────────────────────────────────
# 변경 확인
git status && git diff

# 스테이징 + 커밋 + 푸시 (한 번에)
git add backend/ frontend/ && git commit -m "feat: ..." && git push origin main

# 최근 커밋 로그
git log --oneline -10

# ── Vercel ───────────────────────────────────────────
# 배포 상태 확인 (GitHub API)
GH_TOKEN=$(grep GITHUB_API_KEY .env | cut -d= -f2) gh api repos/yunpapa/yunpapa/deployments
```

---

*AI 영수증 지출 관리 시스템 운용 가이드 v1.0 — 2026-04-13*
